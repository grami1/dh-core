spring:
  jpa:
    hibernate:
      ddl-auto: create
    database: postgresql
  datasource:
    url: jdbc:tc:postgresql:15.2:///dhcore
    username: name
    password: password
  liquibase:
    enabled: false

weather:
  base-url: http://localhost:8084
  key: key

aws:
  region: eu-north-1
  access-key: access_key
  secret-key: secret_key